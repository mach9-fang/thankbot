diff --git a/dist/confetti.browser.js b/dist/confetti.browser.js
index 72b01eda768e94ae2a6d9d792d4f851a1c58c5df..5cf062aa56fc3061e00b66e0fb95dbd2c29ea5a1 100644
--- a/dist/confetti.browser.js
+++ b/dist/confetti.browser.js
@@ -345,7 +345,7 @@
       x: opts.x,
       y: opts.y,
       wobble: Math.random() * 10,
-      wobbleSpeed: Math.min(0.11, Math.random() * 0.1 + 0.05),
+      wobbleSpeed: Math.min(0.055, Math.random() * 0.05 + 0.025),
       velocity: (opts.startVelocity * 0.5) + (Math.random() * opts.startVelocity),
       angle2D: -radAngle + ((0.5 * radSpread) - (Math.random() * radSpread)),
       tiltAngle: (Math.random() * (0.75 - 0.25) + 0.25) * Math.PI,
@@ -385,7 +385,7 @@
       fetti.wobbleX = fetti.x + ((10 * fetti.scalar) * Math.cos(fetti.wobble));
       fetti.wobbleY = fetti.y + ((10 * fetti.scalar) * Math.sin(fetti.wobble));
 
-      fetti.tiltAngle += 0.1;
+      fetti.tiltAngle += 0.05;
       fetti.tiltSin = Math.sin(fetti.tiltAngle);
       fetti.tiltCos = Math.cos(fetti.tiltAngle);
       fetti.random = Math.random() + 2;
@@ -400,6 +400,7 @@
 
     context.fillStyle = 'rgba(' + fetti.color.r + ', ' + fetti.color.g + ', ' + fetti.color.b + ', ' + (1 - progress) + ')';
 
+    context.save();
     context.beginPath();
 
     if (canUsePaths && fetti.shape.type === 'path' && typeof fetti.shape.path === 'string' && Array.isArray(fetti.shape.matrix)) {
@@ -448,22 +449,27 @@
         context.ellipse(fetti.x, fetti.y, Math.abs(x2 - x1) * fetti.ovalScalar, Math.abs(y2 - y1) * fetti.ovalScalar, Math.PI / 10 * fetti.wobble, 0, 2 * Math.PI) :
         ellipse(context, fetti.x, fetti.y, Math.abs(x2 - x1) * fetti.ovalScalar, Math.abs(y2 - y1) * fetti.ovalScalar, Math.PI / 10 * fetti.wobble, 0, 2 * Math.PI);
     } else if (fetti.shape === 'star') {
+      // Tumble like path hearts / ribbon squares: spin with wobble, squash with tilt.
+      context.translate(fetti.x, fetti.y);
+      context.rotate(Math.PI / 10 * fetti.wobble);
+      context.scale(1, Math.max(0.25, Math.abs(fetti.tiltCos)));
+
       var rot = Math.PI / 2 * 3;
       var innerRadius = 4 * fetti.scalar;
       var outerRadius = 8 * fetti.scalar;
-      var x = fetti.x;
-      var y = fetti.y;
+      var x = 0;
+      var y = 0;
       var spikes = 5;
       var step = Math.PI / spikes;
 
       while (spikes--) {
-        x = fetti.x + Math.cos(rot) * outerRadius;
-        y = fetti.y + Math.sin(rot) * outerRadius;
+        x = Math.cos(rot) * outerRadius;
+        y = Math.sin(rot) * outerRadius;
         context.lineTo(x, y);
         rot += step;
 
-        x = fetti.x + Math.cos(rot) * innerRadius;
-        y = fetti.y + Math.sin(rot) * innerRadius;
+        x = Math.cos(rot) * innerRadius;
+        y = Math.sin(rot) * innerRadius;
         context.lineTo(x, y);
         rot += step;
       }
@@ -476,6 +482,7 @@
 
     context.closePath();
     context.fill();
+    context.restore();
 
     return fetti.tick < fetti.totalTicks;
   }
diff --git a/dist/confetti.module.mjs b/dist/confetti.module.mjs
index ed8e521f624fd3040416b5ab5ec620b4379d3986..09fba44501dcf71506f82c019c8e42499b42d09e 100644
--- a/dist/confetti.module.mjs
+++ b/dist/confetti.module.mjs
@@ -346,7 +346,7 @@ var module = {};
       x: opts.x,
       y: opts.y,
       wobble: Math.random() * 10,
-      wobbleSpeed: Math.min(0.11, Math.random() * 0.1 + 0.05),
+      wobbleSpeed: Math.min(0.055, Math.random() * 0.05 + 0.025),
       velocity: (opts.startVelocity * 0.5) + (Math.random() * opts.startVelocity),
       angle2D: -radAngle + ((0.5 * radSpread) - (Math.random() * radSpread)),
       tiltAngle: (Math.random() * (0.75 - 0.25) + 0.25) * Math.PI,
@@ -386,7 +386,7 @@ var module = {};
       fetti.wobbleX = fetti.x + ((10 * fetti.scalar) * Math.cos(fetti.wobble));
       fetti.wobbleY = fetti.y + ((10 * fetti.scalar) * Math.sin(fetti.wobble));
 
-      fetti.tiltAngle += 0.1;
+      fetti.tiltAngle += 0.05;
       fetti.tiltSin = Math.sin(fetti.tiltAngle);
       fetti.tiltCos = Math.cos(fetti.tiltAngle);
       fetti.random = Math.random() + 2;
@@ -401,6 +401,7 @@ var module = {};
 
     context.fillStyle = 'rgba(' + fetti.color.r + ', ' + fetti.color.g + ', ' + fetti.color.b + ', ' + (1 - progress) + ')';
 
+    context.save();
     context.beginPath();
 
     if (canUsePaths && fetti.shape.type === 'path' && typeof fetti.shape.path === 'string' && Array.isArray(fetti.shape.matrix)) {
@@ -449,22 +450,27 @@ var module = {};
         context.ellipse(fetti.x, fetti.y, Math.abs(x2 - x1) * fetti.ovalScalar, Math.abs(y2 - y1) * fetti.ovalScalar, Math.PI / 10 * fetti.wobble, 0, 2 * Math.PI) :
         ellipse(context, fetti.x, fetti.y, Math.abs(x2 - x1) * fetti.ovalScalar, Math.abs(y2 - y1) * fetti.ovalScalar, Math.PI / 10 * fetti.wobble, 0, 2 * Math.PI);
     } else if (fetti.shape === 'star') {
+      // Tumble like path hearts / ribbon squares: spin with wobble, squash with tilt.
+      context.translate(fetti.x, fetti.y);
+      context.rotate(Math.PI / 10 * fetti.wobble);
+      context.scale(1, Math.max(0.25, Math.abs(fetti.tiltCos)));
+
       var rot = Math.PI / 2 * 3;
       var innerRadius = 4 * fetti.scalar;
       var outerRadius = 8 * fetti.scalar;
-      var x = fetti.x;
-      var y = fetti.y;
+      var x = 0;
+      var y = 0;
       var spikes = 5;
       var step = Math.PI / spikes;
 
       while (spikes--) {
-        x = fetti.x + Math.cos(rot) * outerRadius;
-        y = fetti.y + Math.sin(rot) * outerRadius;
+        x = Math.cos(rot) * outerRadius;
+        y = Math.sin(rot) * outerRadius;
         context.lineTo(x, y);
         rot += step;
 
-        x = fetti.x + Math.cos(rot) * innerRadius;
-        y = fetti.y + Math.sin(rot) * innerRadius;
+        x = Math.cos(rot) * innerRadius;
+        y = Math.sin(rot) * innerRadius;
         context.lineTo(x, y);
         rot += step;
       }
@@ -477,6 +483,7 @@ var module = {};
 
     context.closePath();
     context.fill();
+    context.restore();
 
     return fetti.tick < fetti.totalTicks;
   }
diff --git a/src/confetti.js b/src/confetti.js
index 3eec864ebf6d07511b062d77cc3047c2108c6ee2..ee58e01b8c3d00304b7cdb5208a78e5f436fa464 100644
--- a/src/confetti.js
+++ b/src/confetti.js
@@ -342,7 +342,7 @@
       x: opts.x,
       y: opts.y,
       wobble: Math.random() * 10,
-      wobbleSpeed: Math.min(0.11, Math.random() * 0.1 + 0.05),
+      wobbleSpeed: Math.min(0.055, Math.random() * 0.05 + 0.025),
       velocity: (opts.startVelocity * 0.5) + (Math.random() * opts.startVelocity),
       angle2D: -radAngle + ((0.5 * radSpread) - (Math.random() * radSpread)),
       tiltAngle: (Math.random() * (0.75 - 0.25) + 0.25) * Math.PI,
@@ -382,7 +382,7 @@
       fetti.wobbleX = fetti.x + ((10 * fetti.scalar) * Math.cos(fetti.wobble));
       fetti.wobbleY = fetti.y + ((10 * fetti.scalar) * Math.sin(fetti.wobble));
 
-      fetti.tiltAngle += 0.1;
+      fetti.tiltAngle += 0.05;
       fetti.tiltSin = Math.sin(fetti.tiltAngle);
       fetti.tiltCos = Math.cos(fetti.tiltAngle);
       fetti.random = Math.random() + 2;
@@ -397,6 +397,7 @@
 
     context.fillStyle = 'rgba(' + fetti.color.r + ', ' + fetti.color.g + ', ' + fetti.color.b + ', ' + (1 - progress) + ')';
 
+    context.save();
     context.beginPath();
 
     if (canUsePaths && fetti.shape.type === 'path' && typeof fetti.shape.path === 'string' && Array.isArray(fetti.shape.matrix)) {
@@ -445,22 +446,27 @@
         context.ellipse(fetti.x, fetti.y, Math.abs(x2 - x1) * fetti.ovalScalar, Math.abs(y2 - y1) * fetti.ovalScalar, Math.PI / 10 * fetti.wobble, 0, 2 * Math.PI) :
         ellipse(context, fetti.x, fetti.y, Math.abs(x2 - x1) * fetti.ovalScalar, Math.abs(y2 - y1) * fetti.ovalScalar, Math.PI / 10 * fetti.wobble, 0, 2 * Math.PI);
     } else if (fetti.shape === 'star') {
+      // Tumble like path hearts / ribbon squares: spin with wobble, squash with tilt.
+      context.translate(fetti.x, fetti.y);
+      context.rotate(Math.PI / 10 * fetti.wobble);
+      context.scale(1, Math.max(0.25, Math.abs(fetti.tiltCos)));
+
       var rot = Math.PI / 2 * 3;
       var innerRadius = 4 * fetti.scalar;
       var outerRadius = 8 * fetti.scalar;
-      var x = fetti.x;
-      var y = fetti.y;
+      var x = 0;
+      var y = 0;
       var spikes = 5;
       var step = Math.PI / spikes;
 
       while (spikes--) {
-        x = fetti.x + Math.cos(rot) * outerRadius;
-        y = fetti.y + Math.sin(rot) * outerRadius;
+        x = Math.cos(rot) * outerRadius;
+        y = Math.sin(rot) * outerRadius;
         context.lineTo(x, y);
         rot += step;
 
-        x = fetti.x + Math.cos(rot) * innerRadius;
-        y = fetti.y + Math.sin(rot) * innerRadius;
+        x = Math.cos(rot) * innerRadius;
+        y = Math.sin(rot) * innerRadius;
         context.lineTo(x, y);
         rot += step;
       }
@@ -473,6 +479,7 @@
 
     context.closePath();
     context.fill();
+    context.restore();
 
     return fetti.tick < fetti.totalTicks;
   }
